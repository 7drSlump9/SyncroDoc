# SyncroDoc - Environment Configuration
# Copia questo file come .env e sostituisci i valori con quelli appropriati

# ===== OBBLIGATORIO =====
# Chiave segreta per firmare i JWT
# Genera una chiave sicura e casuale (minimo 32 caratteri)
# Comando per generare: openssl rand -base64 32
JWT_SECRET=your_very_secure_jwt_secret_key_here_change_this_in_production

# Chiave segreta per i Refresh Token
# Diversa da JWT_SECRET per maggiore sicurezza
JWT_REFRESH_SECRET=your_refresh_token_secret_key_here_change_this_in_production

# ===== OPZIONALE =====
# Porta su cui il server deve ascoltare (default: 5000)
PORT=5000

# Origine consentita per CORS (default: http://localhost:3000)
# Aggiungi il dominio della tua applicazione frontend in produzione
ALLOWED_ORIGIN=http://localhost:3000

# ===== SICUREZZA =====
# In produzione:
# 1. Usa HTTPS
# 2. Genera chiavi segrete forti e casuali
# 3. Mantieni le chiavi segrete al sicuro (non commitare .env nel repo)
# 4. Usa variabili di ambiente attraverso il provider di hosting (Vercel, Heroku, etc)
